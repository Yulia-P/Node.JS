create database black22

use black22;
create table Users(
 id int primary key AUTO_INCREMENT, 
 username varchar(50),
 password varchar(50))
 
 use black22;
 insert into users(id, username, password) values(1, 'user1', 'user1');